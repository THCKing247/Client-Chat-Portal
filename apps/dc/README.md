# DC App SSO Integration

This directory contains the SSO handler for the DC (Data Center) app (`dc.apextsgroup.com`).

See `../chat/README.md` for integration examples. The implementation is identical - just copy `sso-handler.js` to your DC app and follow the same setup steps.

